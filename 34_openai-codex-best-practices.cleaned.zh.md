# 最佳实践 (Best practices)

Codex 的入门指南以及获得更好结果的可靠实践

如果你刚接触 Codex 或一般的编码智能体 (coding agents)，本指南将帮助你更快地获得更好的结果。它涵盖了使 Codex 在 CLI、IDE 扩展和 Codex 应用程序中更有效的核心习惯，从提示词 (prompting) 和规划到验证、MCP、技能 (skills) 和自动化。

当你把 Codex 视为一个你可以随着时间的推移去配置和改进的团队成员，而不是一个一次性的助手时，它的效果最好。

一个有用的思考方式：从正确的任务上下文开始，使用 `AGENTS.md` 提供持久的指导，配置 Codex 以匹配你的工作流，使用 MCP 连接外部系统，将重复性工作转化为技能，并实现稳定工作流的自动化。

## 强大的首次使用：上下文和提示词

即使你的提示词不够完美，Codex 也已经足够强大以发挥作用。你通常只需很少的设置就可以把一个难题交给它，并依然能得到很好的结果。虽然产生价值不一定需要清晰的提示词，但这确实能让结果更可靠，特别是在大型代码库或高风险任务中。

如果你在大型或复杂的代码库 (repository) 中工作，最大的突破在于为 Codex 提供正确的任务上下文，以及对你想要完成的工作提供清晰的结构。

一个好的默认做法是在你的提示词中包含四件事：

*   **目标 (Goal)：** 你试图改变或构建什么？
*   **上下文 (Context)：** 哪些文件、文件夹、文档、示例或错误对这个任务很重要？你可以使用 @ 提及特定文件作为上下文。
*   **约束 (Constraints)：** Codex 应该遵循什么标准、架构、安全要求或约定？
*   **完成标准 (Done when)：** 在任务完成之前什么应该是真实的？例如测试通过、行为改变，或者一个 bug 不再重现？

这有助于 Codex 保持在范围内，减少假设，并生成更容易审查的工作。

根据任务的难度选择推理级别，并测试最适合你工作流的设置。不同的用户和任务在不同的设置下表现最佳。

*   **Low**（低）：适用于更快、范围明确的任务
*   **Medium** 或 **High**（中或高）：适用于更复杂的更改或调试
*   **Extra High**（极高）：适用于长时间的、智能体化的、需要大量推理的任务

为了更快地提供上下文，你可以尝试在 Codex 应用程序中使用语音听写，口述你希望 Codex 做什么，而不是打字。

## 对于困难任务，先进行规划

如果任务复杂、模糊或难以很好地描述，在它开始编码之前，请要求 Codex 进行规划。

有几种方法很有效：

**使用规划模式 (Plan mode)：** 对于大多数用户来说，这是最简单和最有效的选项。规划模式让 Codex 在实施之前收集上下文、提出澄清问题，并构建一个更强大的计划。使用 `/plan` 或 Shift+Tab 切换。

**让 Codex 采访你：** 如果你对你想要的东西有一个粗略的想法，但不确定如何很好地描述它，请先让 Codex 问你问题。告诉它挑战你的假设，并在编写代码之前将模糊的想法转化为具体的内容。

**使用 `PLANS.md` 模板：** 对于更高级的工作流，你可以配置 Codex 以遵循 `PLANS.md` 或执行计划模板，用于运行时间较长或多步的工作。有关更多详细信息，请参阅执行计划指南。

## 使用 `AGENTS.md` 使指南可复用

一旦某种提示词模式起作用，下一步就是停止手动重复它。这就是 `AGENTS.md` 发挥作用的地方。

把 `AGENTS.md` 想象成智能体的开放格式 README。它会自动加载到上下文中，是编码你和你的团队希望 Codex 在代码库中如何工作的最佳场所。

一个好的 `AGENTS.md` 涵盖：

*   代码库结构和重要目录
*   如何运行项目
*   构建 (Build)、测试和代码检查 (lint) 命令
*   工程约定和 PR (Pull Request) 期望
*   约束和禁止规则
*   “完成”意味着什么以及如何验证工作

CLI 中的 `/init` 斜杠命令是一个快速启动命令，用于在当前目录中生成基础的 `AGENTS.md` 骨架。这是一个很好的起点，但你应该编辑结果，使其与你的团队实际构建、测试、审查和发布代码的方式相匹配。

你可以在不同层级创建 `AGENTS.md` 文件：位于 `~/.codex` 的全局 `AGENTS.md` 用于个人默认设置，代码库级别的文件用于共享标准，以及在子目录中用于本地规则的更具体的文件。如果更靠近当前目录的地方有更具体的文件，则该指南优先。

保持实用性。一个简短、准确的 `AGENTS.md` 比一个充满模糊规则的长文件更有用。从基础开始，然后仅在发现重复错误后才添加新规则。

如果 `AGENTS.md` 开始变得太大，保持主文件简洁，并在涉及规划、代码审查或架构等任务时，引用特定于任务的 markdown 文件。

当 Codex 两次犯同样的错误时，要求它进行回顾并更新 `AGENTS.md`。让指南保持实用并基于真实的摩擦点。

## 为一致性配置 Codex

配置是使 Codex 在会话和界面之间表现更一致的主要方法之一。例如，你可以为模型选择、推理力度、沙盒模式、审批策略、配置文件和 MCP 设置配置默认值。

一个好的起点模式是：

*   在 `~/.codex/config.toml` 中保留个人默认设置（从 Codex 应用程序的 Settings → Configuration → Open config.toml 进入）
*   在 `.codex/config.toml` 中保留特定于代码库的行为
*   仅在一次性情况下使用命令行覆盖（如果你使用 CLI）

`config.toml` 是你定义持久偏好的地方，例如 MCP 服务器、配置文件、多智能体设置和实验性功能。你可以直接编辑它或要求 Codex 为你更新它。

Codex 附带操作系统级别的沙盒隔离，并有两个你可以控制的关键旋钮。审批模式 (Approval mode) 决定 Codex 何时要求你允许运行命令，而沙盒模式 (sandbox mode) 决定 Codex 是否可以在目录中读取或写入，以及智能体可以访问哪些文件。

如果你刚接触编码智能体，请从默认权限开始。默认情况下保持审批和沙盒处于严格状态，然后只有在需求明确时，才对受信任的代码库或特定工作流放宽权限。

请注意，CLI、IDE 扩展和 Codex 应用程序共享相同的配置层。在示例配置页面上了解更多信息。

尽早为你的真实环境配置 Codex。许多质量问题实际上是设置问题，比如错误的工作目录、缺少写入权限、错误的模型默认值，或缺少工具和连接器。

## 通过测试和审查提高可靠性

不要只停留在要求 Codex 进行更改。要求它在需要时创建测试、运行相关的检查、确认结果，并在你接受之前审查该工作。

Codex 可以为你完成这个循环，但前提是它知道“好”的标准是什么。该指南可以来自提示词或 `AGENTS.md`。

这可以包括：

*   为该更改编写或更新测试
*   运行正确的测试套件
*   检查代码风格、格式或类型检查
*   确认最终行为符合请求
*   审查差异 (diff)，寻找错误、回归 (regressions) 或有风险的模式

在 Codex 应用程序中切换差异面板以直接在本地审查更改。点击特定行提供反馈，这些反馈将作为上下文输入到 Codex 的下一个轮次中。

这里的一个有用选项是斜杠命令 `/review`，它为你提供了几种审查代码的方法：

*   对照基础分支进行 PR 风格的审查
*   审查未提交的更改
*   审查某个提交 (commit)
*   使用自定义审查说明

如果你和你的团队有 `code_review.md` 文件并在 `AGENTS.md` 中引用了它，Codex 也可以在审查期间遵循该指南。对于希望在代码库和贡献者之间保持审查行为一致的团队来说，这是一种强大的模式。

Codex 不应该只生成代码。有了正确的说明，它还可以帮助**测试它、检查它和审查它**。

如果你使用 GitHub Cloud，你可以设置 Codex 为你的 PR 运行代码审查。在 OpenAI，Codex 审查 100% 的 PR。你可以启用自动审查，或让 Codex 在你 @Codex 时进行被动审查。

## 使用 MCP 获取外部上下文

当 Codex 需要的上下文存在于代码库外部时，使用 MCP。它允许 Codex 连接到你已经使用的工具和系统，这样你就不必不断地将实时信息复制粘贴到提示词中。

模型上下文协议 (Model Context Protocol, MCP) 是用于将 Codex 连接到外部工具和系统的开放标准。

在以下情况下使用 MCP：

*   所需上下文存在于代码库外部
*   数据经常变化
*   你希望 Codex 使用工具而不是依赖粘贴的说明
*   你需要跨用户或项目的可重复集成

Codex 支持带有 OAuth 的 STDIO 和 Streamable HTTP 服务器。

在 Codex 应用程序中，前往 Settings → MCP servers 查看自定义和推荐的服务器。通常，Codex 可以帮助你安装所需的服务器。你所需要做的就是提问。你也可以在 CLI 中使用 `codex mcp add` 命令，添加带有名称、URL 和其他详细信息的自定义服务器。

仅当它们解锁真实工作流时才添加工具。不要一开始就接入你使用的每个工具。从一两个明显能消除你经常执行的手动循环的工具开始，然后再从那里扩展。

## 将重复性工作转化为技能 (Skills)

一旦工作流变得可重复，就停止依赖长提示词或重复的来回对话。使用技能 (Skill) 将说明打包在 `SKILL.md` 文件中，包含上下文以及 Codex 应持续应用的辅助逻辑。技能在 CLI、IDE 扩展和 Codex 应用程序中均可使用。

保持每项技能都针对一项工作。从 2 到 3 个具体的用例开始，定义清晰的输入和输出，并编写说明，以便清楚指出该技能的作用以及何时使用它。包括用户实际会说的触发短语类型。

不要试图预先覆盖每一个边缘情况。从一个代表性的任务开始，让它运作良好，然后将该工作流转化为一项技能，并从那里进行改进。仅当脚本或额外资产能提高可靠性时才包含它们。

一个好的经验法则：如果你不断重复使用相同的提示词或纠正相同的工作流，它可能应该成为一项技能。

技能对于以下周期性工作特别有用：

*   日志分类 (Log triage)
*   起草发布说明 (Release note drafting)
*   对照检查清单进行 PR 审查
*   迁移规划
*   遥测或事件摘要
*   标准调试流程

`$skill-creator` 技能是用来搭建技能第一个版本骨架的最佳起点，而 `$skill-installer` 技能用来在本地安装它。技能最重要的部分之一是描述 (description)。它应该说明技能的作用以及何时使用。

个人技能存储在 `$HOME/.agents/skills` 中，团队共享技能可以签入到代码库中的 `.agents/skills`。这对于新队员入职特别有帮助。

## 使用自动化 (Automations) 处理重复工作

一旦工作流稳定，你可以安排 Codex 在后台为你运行它。在 Codex 应用程序中，自动化 (automations) 让你为重复性任务选择项目、提示词、节奏和执行环境。

一旦一项任务对你来说变得重复，你可以在 Codex 应用程序的 Automations 选项卡中创建一个自动化。你可以选择它在哪个项目中运行、它运行的提示词（你可以调用技能）以及它运行的节奏。你还可以选择该自动化是在专用的 git 工作树 (worktree) 还是在你的本地环境中运行。了解有关 git 工作树的更多信息。

很好的候选任务包括：

*   总结最近的提交
*   扫描可能的 bug
*   起草发布说明
*   检查 CI 失败
*   生成每日站会总结
*   按计划运行可重复的分析工作流

一个有用的规则是：技能定义方法，自动化定义时间表。如果一个工作流仍然需要大量的指导，首先将它转化为一项技能。一旦它是可预测的，自动化就变成了力量倍增器。

使用自动化进行反思和维护，而不仅仅是执行。回顾最近的会话，总结反复出现的摩擦，并随着时间的推移改进提示词、说明或工作流设置。

## 使用会话控制管理长时间运行的工作

Codex 会话不仅仅是聊天历史。它们是随时间积累上下文、决策和行动的工作线程，因此很好地管理它们对质量有很大影响。

Codex 应用程序的 UI 使线程管理变得最简单，因为你可以固定线程并创建工作树。如果你使用的是 CLI，这些斜杠命令特别有用：

*   `/experimental`：切换实验性功能并添加到你的 `config.toml`
*   `/resume`：恢复保存的对话
*   `/fork`：在保留原始文字记录的同时创建一个新线程
*   `/compact`：当线程变得很长并且你想要早期上下文的摘要版本时。请注意，Codex 确实会自动为你压缩对话
*   `/agent`：当你运行并行智能体并想要在活动的智能体线程之间切换时
*   `/theme`：选择语法高亮主题
*   `/apps`：在 Codex 中直接使用 ChatGPT 应用程序
*   `/status`：检查当前会话状态

每个连贯的工作单元保留一个线程。如果工作仍然是同一个问题的一部分，停留在同一个线程中通常更好，因为它保留了推理轨迹。仅在工作真正分叉时才使用 fork。

使用 Codex 的多智能体 (multi-agent) 工作流将有边界的工作从主线程中卸载。保持主智能体专注于核心问题，并使用子智能体执行探索、测试或分类等任务。

## 常见错误

首次使用 Codex 时要避免的一些常见错误：

*   在提示词中塞满持久规则，而不是将它们移动到 `AGENTS.md` 或一项技能中
*   没有提供如何最好地运行构建和测试命令的细节，从而不让智能体看到它的工作结果
*   在多步骤和复杂的任务中跳过规划
*   在你理解工作流之前给予 Codex 对你计算机的完全权限
*   在同一文件上运行实时线程而不使用 git 工作树
*   在手动可靠之前将重复性任务转化为自动化
*   将 Codex 视为你必须逐步监视的东西，而不是与你自己的工作并行使用它
*   每个项目使用一个线程，而不是每个任务使用一个线程。随着时间的推移，这会导致上下文臃肿和结果变差
