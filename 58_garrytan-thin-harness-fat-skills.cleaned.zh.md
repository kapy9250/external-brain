# 薄框架，厚技能 (Thin Harness, Fat Skills)

Steve Yegge 说，使用 AI 编程智能体 (coding agents) 的人“比今天使用 Cursor 和聊天的工程师的生产力高出 10 到 100 倍，大约是 2005 年 Google 员工生产力的 1000 倍。”

这是一个真实的数字。我见过。我经历过。但当人们听到它时，他们往往会寻找错误的解释：更好的模型。更聪明的 Claude。更多的参数。然而，那些 2 倍生产力的人和 100 倍生产力的人使用的是完全相同的模型。差异不在于智能，而在于架构——而且它完全可以写在一张索引卡上。

在 2026 年 3 月 31 日，Anthropic 意外地将 Claude Code 的全部源代码发布到了 npm 注册表中。512,000 行代码。我阅读了它。它证实了我在 YC 一直在教授的一切：秘密不在于模型。秘密在于包裹模型的那个东西。

实时的代码库上下文。提示词缓存 (Prompt caching)。特制的工具。最小化上下文膨胀。结构化的会话内存。并行的子智能体。这些都没有让模型变得更聪明。但它们在正确的时间，为模型提供了正确的上下文，而没有用噪音淹没它。

那个包装器 (wrapper) 被称为框架 (harness)。每一个 AI 构建者都应该问的问题是：什么应该放入框架中，什么应该放在外面？答案有一个特定的形状。我称之为**薄框架，厚技能 (thin harness, fat skills)**。

瓶颈永远不是模型的智能。模型已经知道如何推理、综合和编写代码。它们失败是因为它们不了解你的数据——你的模式 (schema)、你的约定，以及你问题特定的形状。五个定义可以解决这个问题。

## 1. 技能文件 (Skill files)

技能文件 (skill file) 是一个可重用的 Markdown 文档，用于教模型如何做某事。不是教它做什么——那是用户提供的。技能提供的是**过程 (process)**。

这是大多数人错过的关键见解：**技能文件就像一个方法调用 (method call)**。它接收参数。你用不同的参数调用它。同一个过程，根据你传入的内容，会产生截然不同的能力。

想象一个名为 `/investigate` 的技能。它有七个步骤：确定数据集范围、建立时间线、对每份文档进行日记化提取 (diarize)、综合、正反双方辩论、引用来源。它需要三个参数：`TARGET`（目标）、`QUESTION`（问题）和 `DATASET`（数据集）。把它指向一位安全科学家和 210 万封电子取证邮件，你就得到了一位医学研究分析师，正在确定吹哨人是否被压制。把它指向一家空壳公司和联邦选举委员会 (FEC) 的文件，你就得到了一名法证调查员，正在追踪协同的竞选捐款。

同样的技能。同样的七个步骤。同样的 Markdown 文件。技能描述了一个判断的过程。而调用它的动作则提供了它的整个世界。

这不是提示词工程 (prompt engineering)。这是软件设计，使用 Markdown 作为编程语言，将人类的判断作为运行时 (runtime)。实际上，Markdown 比死板的源代码更完美地封装了能力，因为它用模型已经习惯思考的语言描述了过程、判断和上下文。

## 2. 框架 (The harness)

框架 (harness) 是运行 LLM 的程序。它只做四件事：在循环中运行模型、读取和写入你的文件、管理上下文，以及强制执行安全规则。仅此而已。这就是“薄 (thin)”。

它的反模式 (anti-pattern) 是“厚框架，薄技能”。你肯定见过：40 多个工具定义吃掉了一半的上下文窗口。拥有 2 到 5 秒 MCP 往返时间的神级工具 (God-tools)。将每个端点都变成一个独立工具的 REST API 包装器。三倍的 token 消耗，三倍的延迟，三倍的故障率。

你真正需要的是**专用的、快速而狭窄的工具**。一个在 100 毫秒内完成每个浏览器操作的 Playwright CLI，而不是一个执行 截图-查找-点击-等待-读取 需要 15 秒的 Chrome MCP。那可是 75 倍的速度差异。软件不再需要像艺术品一样被珍视。完全按照你的需要去构建，一丝一毫多余的都不要。

## 3. 解析器 (Resolvers)

解析器 (resolver) 是上下文的路由表。当出现任务类型 X 时，首先加载文档 Y。

技能告诉模型“如何”做。解析器告诉模型加载“什么”以及“何时”加载。一个开发人员修改了一个 prompt。如果没有解析器，他们就直接发布了。有了解析器，模型会首先读取 `docs/EVALS.md`——里面写着：运行评估套件，比较分数，如果准确率下降超过 2%，则回滚并调查。开发人员甚至不知道评估套件的存在。但解析器在正确的时刻加载了正确的上下文。

Claude Code 有一个内置的解析器。每个技能都有一个描述字段，模型会自动将用户意图与技能描述进行匹配。你永远不需要记住 `/ship` 这个命令存在。描述本身就是解析器。

坦白说：我的 `CLAUDE.md` 曾经有 20,000 行。包含了每一个怪癖、每一个模式、我遇到过的每一个教训。简直荒谬透顶。模型的注意力下降了。Claude Code 直接告诉我把它删减掉。解决方案大约只有 200 行——只是指向各种文档的指针。当重要的事情发生时，解析器会加载正确的那一个。两万行的知识，按需获取，而不会污染上下文窗口。

## 4. 潜在空间 vs. 确定性 (Latent vs. deterministic)

你系统中的每一步要么是其中之一，要么是另一个，混淆它们是智能体设计中最常见的错误。

**潜在空间 (Latent space)** 是智能存在的地方。模型读取、解释、决定。判断。综合。模式识别。

**确定性 (Deterministic)** 是信任存在的地方。相同的输入，相同的输出。每一次都一样。SQL 查询。编译代码。算术运算。

一个 LLM 可以为 8 个人安排餐桌座位，并考虑到他们的个性和社交动态。如果你让它为 800 个人安排座位，它会幻觉出一张看起来很合理但完全错误的座位表。那是一个被强行塞进潜在空间的确定性问题（组合优化问题）。最糟糕的系统把错误的工作放在了这条界线的错误一侧。而最好的系统对此毫不留情。

## 5. 日记化提取 (Diarization)

日记化提取 (Diarization) 是使 AI 对真正的知识工作变得有用的步骤。模型阅读关于某个主题的所有内容，并写出一份结构化的档案——从数十或数百份文档中提炼出的一页纸的判断。

没有任何 SQL 查询能产生这个。没有任何 RAG 流水线能产生这个。模型必须真正地去阅读，在脑海中保留矛盾，注意到什么时间发生了什么变化，并综合出结构化的情报。这就是数据库查找和分析师简报之间的区别。

---

这五个概念组成了一个简单的**三层架构**：

**厚技能 (Fat skills)** 位于顶层：Markdown 过程编码了判断、过程和领域知识。这是 90% 价值所在的地方。

**薄的 CLI 框架 (thin CLI harness)** 位于中间：大约 200 行代码。输入 JSON，输出文本。默认只读。

**你的应用程序 (Your application)** 位于底层：QueryDB、ReadDoc、搜索、时间线——确定性的基础。

这个原则是有方向性的。**将智能向上推向技能。将执行向下推向确定性工具。保持框架轻薄。** 当你这样做时，对模型的每一次改进都会自动改善每一项技能，同时确定性层保持完美的可靠性。

---

让我向你展示这五个定义是如何协同工作的。不是在理论上——而是在我们 YC 正在构建的实际系统中。

Chase Center。2026 年 7 月。创业学校 (Startup School) 的六千名创始人。每个人都有一份结构化的申请表、问卷答案、1:1 导师聊天的文字记录，以及公开信号：X 上的帖子、GitHub 提交记录、显示他们交付速度的 Claude Code 记录。

**传统的方法：** 一个由 15 人组成的团队阅读申请，凭直觉做出决定，更新电子表格。在 200 个创始人的规模下，这行得通。在 6,000 人的规模下，它崩溃了。没有哪个人类能在工作记忆中记住那么多档案，并注意到“AI 智能体基础设施”群组的三个最佳候选人分别是拉各斯的开发工具创始人、新加坡的合规性创始人以及布鲁克林的 CLI 工具创始人——而这三个人在他们的 1:1 聊天中用不同的词语描述了完全相同的痛点。

模型可以做到。以下是方法：

**丰富信息 (Enrichment)。** 一个名为 `/enrich-founder` 的技能提取所有数据源，运行丰富程序，进行日记化提取 (diarizes)，并突出显示创始人“所说的”与他们“实际正在构建的”之间的差距。确定性层处理 SQL 查找、GitHub 统计、在演示 URL 上的浏览器测试、社交信号提取、CrustData 查询。一个 cron 任务每晚运行。六千份档案，永远是最新的。

日记化提取的输出捕获了关键字搜索无法找到的信息：

> FOUNDER: Maria Santos
> COMPANY: Contrail
> SAYS: "Datadog for AI agents" (面向 AI 智能体的 Datadog)
> ACTUALLY BUILDING: 80% 的提交都在计费模块。她正在构建一个伪装成可观测性工具的 FinOps (云财务管理) 工具。

那种差距——“所说的”与“实际正在构建的”——需要阅读 GitHub 的提交历史、申请表和导师聊天记录，并在脑海中同时记住这三者。没有任何基于嵌入 (embedding) 的相似度搜索能找到这一点。没有任何关键字过滤器能找到它。模型必须阅读完整的档案并做出判断。（这是放置在潜在空间中的完美决策！）

**匹配 (Matching)。** 这就是“技能即方法调用 (skill-as-method-call)”大放异彩的地方。对同一个匹配技能进行三次调用，得出三种完全不同的策略：

*   `/match-breakout` 接收 1,200 名创始人，按行业亲和力聚类，每间会议室 30 人。Embedding 结合确定性分配。
*   `/match-lunch` 接收 600 人，跨行业进行随机的惊喜匹配，每桌 8 人，没有重复——由 LLM 发明主题，然后由确定性算法分配座位。
*   `/match-live` 处理此刻在场的所有人，最近邻 embedding 搜索，200 毫秒响应，1:1 配对，排除已经见过面的人。

并且模型会做出聚类算法永远无法做出的判断调用：“Santos 和 Oram 都是 AI 基础设施领域的，但他们不是竞争对手——Santos 做的是成本归因，Oram 做的是编排。把他们分在同一组。”或者：“Kim 申请的是‘开发工具’，但他的 1:1 记录显示他正在构建 SOC2 合规自动化。把他移到金融科技/监管科技 (FinTech/RegTech) 组。”

没有任何 embedding 能捕捉到 Kim 的重新分类。模型必须阅读整个档案。

**学习循环 (The learning loop)。** 活动结束后，一个 `/improve` 技能读取 NPS 调查，对中等评价（“OK”）的回复进行日记化提取——不是看差评，而是看“OK”的评价，那些系统几乎奏效但又差了一点的地方——并提取模式。然后它提出新规则，并将它们写回匹配技能中：

*   当参会者说“AI 基础设施”但初创公司的计费代码占比 >80% 时：→ 分类为 FinTech，而不是 AI Infra。
*   当同一组的两名参会者已经互相认识时：→ 惩罚这种接近度。优先考虑新颖的介绍。

这些规则被写回技能文件。下一次运行会自动使用它们。**技能重写了它自己。**

7 月活动的“OK”评级占 12%。下一次活动：4%。技能文件学会了“OK”实际意味着什么，系统在没有任何人重写代码的情况下变得更好了。

相同的模式随处可见：检索、阅读、日记化提取、计数、综合。然后：调查、调查研究、日记化提取、重写技能。

如果你想知道 2026 年最有价值的循环是什么，那就是这些。我们可以将它们应用于现存的每一门学科和知识工作的各行各业。

我最近发了一条我在给 OpenClaw 的指令，引发的共鸣超出了我的预期：

> 你不允许做一次性的工作。如果我要求你做某事，并且它是那种需要再次发生的事情，你必须：第一次在 3 到 10 个项目上手动执行。向我展示输出。如果我批准了，将其编纂到技能文件 (skill file) 中。如果它应该自动运行，把它放到 cron 任务里。测试标准是：如果同样的事情我必须要求你做两次，你就失败了。

一千个赞和两千五百次收藏。人们以为那是一个提示词工程技巧。不是的。这就是我一直在描述的架构。你编写的每一个技能都是对你系统的永久性升级。它永远不会退化。它永远不会忘记。它在你睡觉的时候在凌晨 3 点运行。当下一个新模型发布时，每一个技能都会瞬间变得更好——潜在步骤中的判断力提升了，而确定性步骤依然保持着完美的可靠性。

这就是你如何获得 Yegge 所说的 100 倍生产力的方法。不是靠一个更聪明的模型。而是靠**厚技能，薄框架，以及将一切编纂成规程的纪律**。

系统会产生复利。构建一次，它将永远运行。