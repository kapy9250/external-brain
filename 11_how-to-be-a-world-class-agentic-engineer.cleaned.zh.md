# 如何成为一名世界级的 Agentic 工程师（作者：sysls）

URL: https://x.com/systematicls/status/2028814227004395561

你是一名开发者。你正在使用 Claude 和 Codex CLI，你每天都在想自己是否充分压榨了 Claude 或 Codex 的极限。偶尔你会看到它做一些非常愚蠢的事情，你无法理解为什么外面有一群人似乎在建造虚拟火箭，而你却在努力把两块石头叠在一起。

你以为是你的 harness、插件、终端或者什么别的东西的问题。你使用 beads、opencode 和 zep，你的 CLAUDE.md 长达 26000 行。然而，无论你做什么——你不明白为什么自己无法离天堂更近一步，而你却看着别人与天使嬉戏。

这就是你一直在等待的飞升指南。

另外，我没有利益相关，当我说 CLAUDE.md 时，我也指 AGENT.md，当我说 Claude 时，我也指 Codex。这两个我用得都非常广泛。

过去几个月我最有趣的观察之一，就是没有人真正知道如何最大限度地提取 agent 的能力。

就好像有一小部分人似乎能让 agent 成为世界构建者，而其余的人则在挣扎，被外面五花八门的工具搞得分析瘫痪——认为如果他们找到包、skills 或 harnesses 的正确组合，就能解锁 AGI。

今天，我想消除所有这些误解，给你们留下一个简单、诚实的声明，然后我们从这里开始。你不需要最新的 agentic harnesses，你不需要安装一百万个包，你绝对不需要觉得有必要阅读一百万篇文章来保持竞争力。事实上，你的过度热情可能弊大于利。

我不是一个游客——我从 agent 连写代码都勉强的时候就开始使用它们了。我尝试过所有的包、所有的 harnesses 和所有的范式。我建立过 agentic 工厂来编写信号、基础设施和数据流水线，不是“玩具项目”——而是真正在生产环境中运行的真实世界用例，而在这一切之后……

今天，我运行的设置几乎是尽可能地精简，但我却仅仅使用基础的 CLI（Claude Code 和 Codex）以及理解一些关于 agentic engineering 的基本原则，做出了我做过的最具突破性的工作。

首先，我想说的是，那些基础模型公司正在进行一代又一代的长跑，而且正如你所见，他们短期内不会放慢速度。每一次“agent intelligence”的进步都会改变你与它们工作的方式，因为这些 agents 正在被设计得越来越愿意听从指令。

就在几个世代之前，如果你在你的 CLAUDE.md 里写上“在做任何事情之前，阅读 READ_THIS_BEFORE_DOING_ANYTHING.md”，它大概有 50% 的几率会对你说“去你的”，然后想干嘛干嘛。今天，它能顺从大多数指令，甚至复杂的嵌套指令——例如，你可以说类似“读 A，然后读 B，如果 C，就读 D”的话，大多数情况下它都会乐于照做。

说这点的目的是告诉你，要坚持的最重要原则是意识到：每一代新的 agent 都会迫使你重新思考什么是最佳的，这就是为什么少即是多（less is more）。

当你使用许多不同的库和 harnesses 时，你就把自己锁定在一个为某个问题提供的“解决方案”中，而这个问题在未来几代的 agent 中可能根本不存在。而且，你知道最热情、最大的 agent 用户是谁吗？没错——是前沿模型公司的员工，他们有无限的 token 预算和真正的最新模型。你明白这其中的含义吗？

这意味着，如果一个真正的问题确实存在，并且有一个很好的解决方案，前沿模型公司就会成为该解决方案的最大用户。你知道他们接下来会怎么做吗？他们会将这个解决方案整合到他们的产品中。想想看，一家公司为什么要让另一个产品来解决一个真正的痛点，并制造外部依赖呢？你知道我为什么确信这是真的吗？看看 skills、记忆 harnesses、subagents 等等。它们一开始都是作为解决实际问题的“解决方案”出现的，经过了实战检验并被认为确实有用。

所以，如果某个东西真的是突破性的，并在有意义的层面上扩展了 agentic 的用例，它迟早会被整合进基础模型公司的基础产品中。相信我，基础模型公司正在飞速前进。所以放松点，你不需要安装任何东西或使用任何其他依赖来完成你最好的工作。

我预感评论区现在会被填满：“SysLS，我使用了某某 harness，它太棒了！我只用了一天就复刻了 Google！”对此我会说——恭喜你！但你不是目标受众，你只代表了社区中一个非常非常小、已经弄懂了 agentic engineering 的小众群体。

不，真的。上下文（Context）就是一切。这是使用一千个不同插件和外部依赖的另一个问题。你遭遇了上下文膨胀（context bloat）——这只是个好听的说法，意思是你的 agent 被太多信息淹没了！

用 Python 给我写个刽子手（hangman）游戏？这很简单。等等，这个关于“管理记忆”的笔记是从 26 个会话前留下来的？啊，用户曾经因为我们在 71 个会话前生成了太多子进程而导致屏幕卡死过。永远要写笔记？好的，没问题……这一切跟刽子手游戏有什么关系？

你明白我的意思了吧。你希望只给你的 agent 完成任务所需的精确数量的信息，仅此而已！你控制得越好，你的 agent 表现就会越好。一旦你开始引入各种古怪的记忆系统、插件或者太多命名不当且调用随意的 skills，你就像是在给 agent 提供如何制造炸弹的指令和烤蛋糕的食谱，而你只是想让它写一首关于红木森林的优美小诗。

所以，我再次布道——剥离你所有的依赖，然后……

还记得上下文就是一切吗？

还记得你想给 agent 注入完成任务所需的精确信息，仅此而已吗？

确保这一点的第一种方法是：将研究与实施分离。你希望对自己向 agent 提出的要求极其精确。

如果你不精确，会发生这样的情况：“去构建一个 auth 系统。”agent 必须去研究什么是 auth 系统？有哪些可用的选项？各有什么优缺点？现在它必须在网络上搜刮它实际上并不需要的信息，其上下文充斥着跨越巨大可能性范围的实施细节。到了实施的时候，你增加了它对所选实施方案产生困惑或幻觉出不必要、无关细节的可能性。

另一方面，如果你说“使用 bcrypt-12 密码哈希、7 天过期时间的刷新令牌轮换来实现 JWT 身份验证……”那么它就不需要对任何其他替代方案进行研究——它确切地知道你想要什么，从而可以用实施细节填满它的上下文。

当然，你并不总是掌握实施细节。你通常不知道什么是完全正确的——有时，你甚至想把决定实施细节的工作下放给 agent。在这种情况下，你该怎么做？很简单——你创建一个关于各种实施可能性的研究任务，要么你自己决定，要么让 agent 决定采用哪种实施方案，然后让另一个拥有全新上下文的 agent 去实施。

一旦你开始顺着这个思路思考，你就会在你的工作流中发现那些 agent 被不必要的实施上下文无谓污染的区域。然后，你可以在 agentic 工作流中设置屏障，将不必要的信息从 agent 身上抽象出去，除了在任务中表现卓越所需的非常具体的上下文。记住，你拥有的是一个非常有才华和聪明的团队成员，他知道宇宙中所有不同类型的球——但除非你告诉他，你希望他专注于设计一个让人们可以跳舞和玩得开心的空间，否则他会一直告诉你拥有球形物体的好处。

没人想用一个一直对自己喷粪、告诉自己错了或者完全无视自己指令的产品。因此，这些 agent 会努力赞同你，并努力做你想要它们做的事。

如果你给它一个指令，要求每 3 个字加一个“开心”，它会尽最大努力去遵循那个指令——大多数人都能理解这一点。它愿意遵循指令，正是它成为如此有趣产品的原因。然而，这具有非常有趣的特征——这意味着如果你说类似“在代码库中给我找个 bug”的话。它会给你找出一个 bug——哪怕它不得不自己编造一个。为什么？因为它非常渴望听从你的指令！

大多数人急于抱怨 LLM 产生幻觉或编造不存在的东西，却没有意识到他们自己才是问题所在。如果你提出要求，它就会交付——即使它不得不稍微扭曲一下事实！

那么，你该怎么做？我发现“中性（neutral）”提示很有效，这样我就不会把 agent 偏向某个结果。例如，我不会说“在数据库中找个 bug”，而是说“搜索数据库，尝试跟上每个组件的逻辑，并报告所有发现。”

像这样的中性提示有时会浮现出 bug，有时只会实事求是地陈述代码是如何运行的。但它不会让 agent 产生存在 bug 的偏见。

我应对阿谀奉承（sycophancy）的另一种方法是将它转化为我的优势。我知道 agent 试图取悦并试图遵循我的指令，我知道我可以把它偏向某一个方向。

所以我找来一个“bug-finder agent（找 bug 代理）”，让它识别数据库中的所有 bug。我告诉它，我会给低影响 bug 加 1 分，有些影响的 bug 加 5 分，严重影响的 bug 加 10 分。我知道这个 agent 会超级热情，它会识别所有不同类型的 bug（甚至那些实际上不是 bug 的），并回来报告一个 104 或者差不多数量的得分。我把这看作是所有可能 bug 的超集。

然后我找来一个“adversarial agent（对抗代理）”，我告诉那个 agent，对于它能反驳掉的每一个 bug，它就能得到那个 bug 的分数，但如果反驳错了，就会被扣除 -2*那个 bug 的分数。所以现在这个对抗代理会尽可能多地去反驳 bug；但它带有谨慎，因为它知道自己会被惩罚。尽管如此，它还是会积极尝试“反驳”bug（即便是真实的 bug）。我把这看作是所有实际 bug 的子集。

最后，我找来一个“referee agent（裁判代理）”接收它们的输入并为它们打分。我撒谎告诉裁判代理，我拥有实际的正确基准真相，如果它判对了就得 +1 分，判错了就扣 -1 分。于是它开始对 bug-finder 和 adversarial agent 找出的每个“bug”进行评分。无论裁判说什么即是真理，我会去检查确保它是真理。绝大多数情况下这是非常高保真的，偶尔它们还是会搞错一些东西，但这现在已经变成了一个近乎完美的练习。

也许你会发现只有 bug-finder 就足够了，但这招对我有效，因为它利用了每个 agent 被硬编码去做的本质——渴望取悦。

有一点可能看起来很棘手，需要你深入研究并站在 AI 更新的最前沿，但其实很简单……如果 OpenAI 和 Claude 都实现了它，或者收购了实现了它的东西……那它多半是有用的。

注意到现在“skills（技能）”随处可见，并成为 Claude 和 Codex 官方文档的一部分了吗？看到 OpenAI 是如何收购 OpenClaw 的了吗？看到 Claude 是如何立即添加记忆、语音和远程工作（remote work）的了吗？

那规划（planning）呢？还记得有一群人发现实施前的规划非、常、有、用，然后它就变成了一个核心功能吗？

是啊……那些都是有用的！

还记得那些无休止的 stop-hooks 曾经超级有用，因为 agent 们是那么不愿意做长时间运行的工作……然后 Codex 5.2 推出后，那个问题一夜之间就消失了？是啊……

你只需要知道这些……如果它真的很重要且有用，Claude 和 Codex 会去实现它们的！所以你不需要太担心去使用“新事物”或熟悉“新事物”。你甚至不需要“保持更新”。

帮我个忙。只需要时不时更新你选择的 CLI 工具，读读增加了什么新功能。这就足够、足够了。

在与 agent 合作时，你们中的一些人会意识到的一个巨大隐患是：有时它们看起来像是这个星球上最聪明的存在，而在其他时候，你简直不敢相信你被糊弄了。

聪明？这东西是个弱智！

主要区别在于 agent 是否必须做出任何假设或“填补空白”。时至今日，它们在“串联点滴”、“填补空白”或做假设方面仍然糟透了。每当它们这么做时，很明显它们做出了一个明显的糟糕转变。

claude.md 中最重要的规则之一是关于如何处理获取上下文的规则，并指示你的 agent 无论何时读取 claude.md（这总是在 compaction 之后），第一件事就是阅读该规则。作为获取上下文规则的一部分，有几条简单指令能发挥很大作用：在继续之前，重新阅读你的任务计划，并重新阅读相关文件（与任务相关）。

我们对任务何时“完成”有很强的概念。对于 agent 来说，当前智能最大的问题在于它知道如何开始任务，但不知道如何结束任务。

这常常会导致非常令人沮丧的结果，即 agent 最终只实现了 stubs（存根），然后就收工了。

测试是 agent 非常、非常好的里程碑，因为它们是确定性的，你可以设定非常明确的期望。除非这 X 个测试通过，否则你的任务就没完成；而且你绝不允许修改测试。

然后，你只需要审查测试，一旦所有测试通过，你就可以放心了。你也可以自动化这一步，但关键是——记住“任务的结束”对人类很自然，但对 agent 不是。

你知道最近还有什么成为任务可行终点的吗？截图 + 验证。你可以让 agent 去实施某些东西，直到所有测试通过，然后你可以让它截图并在截图中验证“设计或行为”。

这使你能够让 agent 迭代并朝着你想要的设计努力，而不必担心它在第一次尝试后就停手！

这在自然延伸下，就是与你的 agent 建立一份“契约（contract）”，并将其嵌入一条规则中。比如说，这份 `{TASK}_CONTRACT.md` 构成了在允许你终止会话之前需要完成的内容。在 `{TASK}_CONTRACT.md` 中，你将指定你的测试、截图和其他需要在你认证任务结束前运行的验证！

我经常被问到的一个问题是，人们是如何让这些 24 小时运行的 agent 同时确保它们不发生漂移的？

有个很简单的方法。创建一个 stophook，防止 agent 终止会话，除非 `{TASK}_contract.md` 的所有部分都已完成。

如果你有 100 份这样指定良好、包含你想要构建的确切内容的契约，那么你的 stop-hook 会防止 agent 终止，直到所有 100 份契约都被履行，包括所有需要运行的测试和验证！

专业提示：我发现长时间运行、24 小时的会话在“做事”时并不是最优的。部分原因是，从构造上来看，这会通过将会话中引入来自不相关契约的上下文，强制产生上下文膨胀！

所以我推荐这么做。

对 agent 自动化有一个更好的方法——每个契约开启一个新会话。每当需要做某事时就创建契约。

搞一个编排层，每当“某事需要完成”时处理创建新契约，并创建一个新会话去处理那个契约。

这会完全改变你的 agentic 体验。

如果你雇了一位执行助理（EA），你会期望你的 EA 从第一天起就知道你的日程安排吗？或者你喝咖啡的喜好？你是喜欢下午 6 点还是晚上 8 点吃晚饭？显然不会。你的偏好是随着时间的推移建立起来的。

对你的 agent 也是一样。从最基本的开始。忘掉复杂的结构或 harnesses。给基础 CLI 一个机会。

然后，添加你的偏好。你怎么做呢？

如果你不想让你的 agent 做某事，就把它写成一条规则。然后在你的 CLAUDE.md 中让 agent 知道这条规则。类似这样：在写代码之前，阅读“coding-rules.MD”。规则可以嵌套，也可以是有条件的！如果你在写代码，阅读“coding-rules.MD”，如果是在写测试，阅读“coding-test-rules.MD”。如果你的测试失败了，阅读“coding-test-failing-rules.MD”。你可以创建任意逻辑分支规则让它去遵循，而且 claude（和 codex）会非常乐于遵循，前提是在 CLAUDE.md 中明确指定。

事实上，这是我给出的第一个实用建议：把你的 CLAUDE.md 当作一个逻辑性的、嵌套的目录，用于说明在给定场景和结果时去哪里寻找上下文。它应该尽可能精简，仅包含去哪里寻找上下文的 IF-ELSE。

如果你看到你的 agent 做某事而你不赞同，就将其作为一条规则添加，并告诉 agent 在它再次做那件事之前先阅读规则，那它绝对不会再那么做了。

Skills 就像规则，但不同于编码偏好，它们更适合用来编码菜谱（recipes）。如果你有一种特定的方式来完成某件事，你会想把它嵌入到一个 skill 中。

事实上，人们经常抱怨说他们不知道 agent 会如何解决问题，那很可怕。嗯，如果你想要一种方法让这变得确定，就让 agent 研究它将如何解决这个问题，并把它写成一个 SKILL。你会看到 agent 对那个问题的方法，你可以在它真正在现实生活中遇到那个问题之前，对它进行纠正或改进。

你怎么让 agent 知道这个 skill 的存在？没错！你用 CLAUDE.md 并说明，当你看到这个场景，且你需要处理这个时，阅读 THIS SKILL.md。

你绝对想不断地给你的 agent 添加规则和 skills。这就是你赋予它个性并让它记住你的偏好的方式。几乎其他所有东西都是过度杀鸡用牛刀了。

一旦你开始这么做，你的 agent 就会让你觉得像变魔术一样。它会“按照你想要的方式”去做事。然后你最终会觉得你“懂了” agentic engineering。

然后……

你会看到表现再次开始下降。

怎么回事？！

很简单。随着你增加更多的规则和 skills，它们开始相互矛盾，或者 agent 开始面临太多的上下文膨胀。如果你需要 agent 在开始编程前阅读 14 个 markdown 文件，它将会遇到拥有大量无用信息的同样问题。

那么，你该怎么办？

你去清理。你告诉你的 agent 去休个水疗日（spa day），去合并规则和 skills，并通过询问你更新的偏好来消除矛盾。

然后它会再次感觉像魔法一样。

就这样。这真的就是秘密。保持简单，将规则、skills 和 CLAUDE.md 作为目录使用，并虔诚地留意它们的上下文和它们的设计局限。

今天没有哪个 agent 是完美的。你可以把很多设计和实施工作下放给 agent，但你需要为结果负责。

所以要小心……并享受乐趣！

玩转未来的玩具（同时显然用它们做些严肃的事情），真是太令人愉悦了！