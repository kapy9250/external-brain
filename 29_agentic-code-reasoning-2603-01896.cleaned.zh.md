# Agentic Code Reasoning (智能体代码推理) [v2]

**作者**: Shubham Ugare, Satish Chandra (Meta, USA)
**日期**: 2026年3月5日 (v2)

## 摘要

LLM 智能体能否在不执行代码的情况下，探索代码库并推理代码的语义？我们研究了这种能力，称之为 **智能体代码推理 (agentic code reasoning)**，并引入了 **半形式化推理 (semi-formal reasoning)**：一种结构化的提示方法，要求智能体构建明确的前提，追踪执行路径，并推导出形式化结论。

与非结构化的思维链 (chain-of-thought) 不同，半形式化推理充当了“证书”的作用：智能体不能跳过用例或提出没有证据支持的主张。我们在三个任务（补丁等价性验证、缺陷定位和代码问答）上进行了评估，结果表明，半形式化推理在所有任务上都一致地提高了准确率。

*   **补丁等价性**：在精选样例上，准确率从 78% 提高到 88%；在真实世界智能体生成的补丁上达到 93%，接近无执行 RL 奖励信号所需的可靠性（比 single-shot 基线高 7 个百分点）。
*   **代码问答** (RubberDuckBench)：准确率达到 87%（比标准 agentic 推理高 9 个百分点）。
*   **缺陷定位** (Defects4J)：Top-5 准确率比标准推理提高了 5 个百分点。

这些结果表明，结构化的智能体推理无需执行即可实现有意义的语义代码分析，为 RL 训练流水线、代码审查和静态程序分析开启了实际应用的可能性。

## 1. 简介

验证智能体的推理是否正确是一个基本挑战。现有的无需执行的验证方法要么是无结构推理（容易让模型在没有明确理由的情况下做出主张），要么是完全形式化验证（如 Lean 或 Coq，但对于跨多种框架和语言的任意代码库来说并不实用）。

我们引入了半形式化推理：提供结构化的推理模板，要求为每一个主张提供明确的证据。模板要求：陈述前提、追踪相关代码路径、提供形式化结论。这种格式自然地鼓励过程间推理（interprocedural reasoning），因为追踪程序路径需要智能体跟随函数调用，而不是靠猜测。

## 2. 评估任务

1.  **补丁等价性验证**：给定两个针对同一规范的补丁，它们是否产生相同的测试结果？
2.  **代码问答** (RubberDuckBench)：回答有关代码库行为、项目特定逻辑、库语义和边缘情况的问题。
3.  **缺陷定位** (Defects4J)：仅给定失败的测试，识别出有缺陷的确切代码行。

## 3. 半形式化推理机制

*   **标准推理**：给智能体一个最小的提示，让它自然语言解释并得出结论。这种方式很快，但智能体会做出未经证实的断言。
*   **半形式化推理**：通过提示中的结构化模板强制执行。它强迫智能体在得出结论之前先收集证据。在补丁等价性中，前提描述每个补丁修改了什么，主张追踪每个测试的行为；在缺陷定位中，前提描述可疑的代码区域，主张追踪每个区域是否会导致测试失败。

## 4. 实验结果

*   **补丁等价性 (Curated Dataset)**: Opus-4.5 模型使用标准推理准确率为 78.2%，使用半形式化推理准确率达到 88.8%。
*   **补丁等价性 (LLM Verifier With Tests)**: Opus-4.5 使用半形式化推理达到 93.0% 准确率。单次调用 (Single Call) 的准确率在 80-87% 之间，证实了许多验证任务需要探索补丁上下文之外的代码。
*   **缺陷定位 (Defects4J)**: 在 Agentic 模式下，Opus-4.5 使用标准推理的 Top-5 准确率为 60.5% (All) / 81.4% (Any)，而使用半形式化推理的 Top-5 准确率为 72.1% (All) / 88.4% (Any)（在严格指标下提高了近 12 个百分点）。在 100 个随机漏洞的更大规模验证中，Top-5 准确率也提升了近 5 个百分点。
*   **代码问答**: 在 RubberDuckBench 上，Opus-4.5 的准确率从 78.3% (标准 agentic) 提升到 87.0% (半形式化 agentic)。

## 5. 结论

研究表明，LLM 智能体无需执行即可执行有意义的语义代码分析。结构化推理模板提供了一种补充传统静态分析的方法：我们不需要在特定算法中硬编码分析逻辑，而是可以使用特定于任务的格式来提示智能体，这种格式可以跨语言和框架泛化。
