# LLM 知识库 (LLM Knowledge Bases)

最近我发现有一件事非常有用：使用 LLM 为各种感兴趣的研究主题构建个人知识库。通过这种方式，我最近产生的大部分 token 吞吐量较少用于操作代码，而更多地用于操作知识（以 markdown 和图像的形式存储）。最新的 LLM 在这方面非常擅长。因此：

**数据摄取 (Data ingest)：** 我将源文档（文章、论文、repos、数据集、图像等）索引到一个 `raw/` 目录中，然后我使用 LLM 增量式地“编译”一个 wiki，它实际上就是目录结构中 `.md` 文件的集合。这个 wiki 包含了 `raw/` 中所有数据的摘要、反向链接，然后将数据分类为概念，为它们撰写文章，并将它们全部链接起来。为了将网络文章转换为 `.md` 文件，我喜欢使用 Obsidian Web Clipper 扩展，然后我还使用一个快捷键将所有相关图像下载到本地，以便我的 LLM 能够轻松引用它们。

**IDE：** 我使用 Obsidian 作为 IDE 的“前端”，在那里我可以查看原始数据、编译后的 wiki 以及衍生的可视化结果。需要强调的是，LLM 编写并维护着 wiki 的所有数据，我很少直接触碰它。我尝试过几个 Obsidian 插件，以其他方式渲染和查看数据（例如用于幻灯片的 Marp）。

**问答 (Q&A)：** 有趣的地方在于，一旦你的 wiki 足够大（例如，我关于最近一些研究的 wiki 有约 100 篇文章和大约 40 万字），你就可以向你的 LLM 智能体询问针对这个 wiki 的各种复杂问题，它会去研究答案等等。我原以为我必须求助于花哨的 RAG（检索增强生成），但 LLM 在自动维护索引文件和所有文档的简短摘要方面表现得相当好，而且在目前这种约摸算是较小的规模下，它可以相当轻松地阅读所有重要的相关数据。

**输出 (Output)：** 我不喜欢在文本/终端中获取答案，而是喜欢让它为我渲染 markdown 文件，或者幻灯片（Marp 格式），或者 matplotlib 图像，然后我会在 Obsidian 中再次查看所有这些内容。你可以想象根据查询生成许多其他可视化的输出格式。通常，我最终会将输出“归档”回 wiki 中，以增强它以供进一步查询。因此，我自己的探索和查询总是能在知识库中不断“积累”。

**代码检查与清理 (Linting)：** 我在 wiki 上运行了一些 LLM 的“健康检查”，比如查找不一致的数据，插补缺失的数据（使用网络搜索器），为新的文章候选找到有趣的联系等，从而逐步清理 wiki 并提高其整体数据完整性。LLM 在建议提出进一步的问题和深入研究方面非常出色。

**额外工具 (Extra tools)：** 我发现自己正在开发额外的工具来处理数据，例如，我借助 vibe coding 编写了一个在 wiki 上运行的微型且朴素的搜索引擎，我既可以直接使用它（在 web ui 中），但也更经常想通过 CLI 将其作为工具移交给 LLM 以进行更大的查询。

**进一步的探索 (Further explorations)：** 随着 repo 的增长，自然会想到生成合成数据并进行微调 (finetuning)，让你的 LLM 在其权重中“知道”这些数据，而不仅仅是在上下文窗口 (context windows) 中。

**总结 (TLDR)：** 收集来自一定数量来源的原始数据，然后由 LLM 编译成 `.md` 的 wiki，再由 LLM 驱动的各种 CLI 对其进行操作以进行问答并增量增强 wiki，所有这一切都可以在 Obsidian 中查看。你几乎不需要手动编写或编辑 wiki，这是 LLM 的领域。我认为这里有空间诞生一款令人惊叹的新产品，而不是一堆拼凑起来的脚本。