“我们需要的不只是有技能的 agent，而是技能可以随着时间持续改进的 agent。”

Agent 技能会长期存在，但有一个核心问题一直没被真正解决：技能通常是静态的，而环境是持续变化的。

一个几周前还有效的技能，可能会在代码库变化、模型行为漂移、或用户任务类型变化后悄悄失效。在很多系统里，这类问题往往要等到结果明显变差，甚至彻底报错，才会被发现。

缺失的关键在于：把技能当作“活的系统组件”，而不是固定不变的提示词文件。

这正是该思路的重点：不仅要更好地存储和路由技能，还要让技能在失败或表现不佳时能够自动改进。

[![Image 1](./assets/self-improving-skills-for-agents/image-01.png)](https://x.com/tricalt/article/2032179887277060476/media/2032166214189916161)

过去，技能流程通常是：

1. 写一个 prompt
2. 存进技能文件夹
3. 需要时调用

这在 demo 阶段效果不错，但很快会碰到同样的问题：

- 某个技能被过度选择
- 某个技能看起来很好，但实战经常失败
- 某条指令反复失效
- 工具调用因环境变化而中断

更麻烦的是，团队往往难以判断问题到底出在路由、指令本身，还是工具调用环节，最后只能靠人工排查和维护。

这个实现的价值在于把闭环补齐，让技能具备“随运行数据持续自我优化”的能力。

典型技能目录可能是：

```text
my_skills/
  summarize/
  bug-triage/
  code-review/
```

借助 cognee，可以给技能增加更清晰的结构与语义信息（任务模式、摘要、关系等），从而让检索与路由更精准。这些都可以通过 cognee 的 `Custom DataPoint` 存储。

![Image 2](./assets/self-improving-skills-for-agents/image-03.jpg)

如果系统不记录技能执行时发生了什么，技能就不可能真正改进。因此每次执行后都应记录：

- 尝试了什么任务
- 选择了哪个技能
- 是否成功
- 出现了什么错误
- 用户反馈（如有）

有了这层观测，失败就变成可分析对象。随着失败样本积累，系统可以回看该技能关联的执行历史（历史运行、反馈、工具错误、相关任务模式），识别重复出现的失效因素，并据此提出更有针对性的改进。

`runs → repeated weak outcomes → inspection`

当证据显示技能表现不佳时，系统可以提出对技能指令的修订建议。这个建议可以由人工审核，也可以自动应用。

修订内容可能包括：

- 收紧触发条件
- 增加缺失约束
- 重排步骤顺序
- 调整输出格式

到这一步，技能就不再只是静态 prompt 文件，而更像可演进组件。与其凭感觉去改 `SKILL.md`，不如基于真实执行证据提出补丁。

当然，自我修改不能无约束。每次修订都必须评估：

- 结果是否真的提升？
- 失败率是否下降？
- 是否在其他地方引入新问题？

因此闭环不应止于：

- `observe → inspect → amend`

而应是：

- `observe → inspect → amend → evaluate`

如果修订没有带来可衡量改进，就应回滚。由于每次变更都记录了动机与结果，原始指令不会丢失，自我优化也能保持可审计、可追踪，而不是失控变更。

当评估确认改进成立，修订才成为技能的新版本。

[![Image 3](./assets/self-improving-skills-for-agents/image-02.jpg)](https://x.com/tricalt/article/2032179887277060476/media/2032172060462436352)

在模型、代码库与任务持续变化的背景下，技能不可能一直静态可用。这个方案给出了一个务实路径：在不牺牲控制与监督的前提下，实现技能的自动化持续改进。
