#  Harness 工程就是控制论

阅读 OpenAI 的 [Agent SDK](https://openai.com/index/introducing-agents-sdk/) 时，我始终有一种说不清的感觉。直到突然意识到：我以前见过这种模式。不是一次——而是三次。

第一次是 1780 年代的**离心调速器**。在此之前，工 人站在蒸汽机旁手动调节阀门。之后，配重飞球机制感知转速并自动调节阀门。工人没有消失。工作变了：从转动阀门变成了设计调速器。

第二次是 **Kubernetes**。你声明期望状态——三个副本、这个镜像、这些资源限制。控制器持续观察实际状态。当它们出现分歧时，控制器进行调谐：重启崩溃的 Pod、扩展副本、回滚错误的部署。工程师的工作从重启服务转变为编写系统赖以调谐的规范。

第三次就是现在。OpenAI 描述的工程师不再编写代码。相反，他们设计环境、构建反馈循环、编纂架构约束——然后由 Agent 编写代码。

[Claude Code](https://openai.com/index/claude-code/)：100% AI 生成，零手动编写。他们称之为“Harness 工程”。

**每一次都是同样的模式。**

Norbert Wiener 在 1948 年将其命名为：**控制论**（Cybernetics），源自希腊语 κυβερνήτης —— 舵手。你不再转动阀门。你来掌舵。

每一次这种模式的出现，都是因为有人构建了足够强大的传感器和执行器，能够在该层级闭合反馈回路。

代码库有反馈回路，但只在较低层级。编译器在语法层面闭合回路。测试套件在行为层面闭合回路。Lint 工具在代码风格层面闭合回路。这些是真正的控制论控制——但它们只在能够机械检查的属性上操作。能编译吗？能通过测试吗？符合规则吗？

而在这之上的一切——*这个改动符合系统架构吗？这是正确的方法吗？这个抽象会在代码库增长时导致问题吗？*——没有传感器，也没有执行器。只有人类能在那个层面上操作，两边都是：判断质量和编写修复方案。

LLM 瞬间改变了这两点。它们能在人类曾经拥有的层面上感知——并在同一层面上行动：重构模块、重新设计不一致的接口、根据真正重要的契约重写测试套件。首次，反馈回路可以在做出重要决策的地方闭合。

但闭合回路是必要条件，而非充分条件。瓦特的调速器需要调整。Kubernetes 控制器需要正确的规范。而在你的代码库上工作的 LLM 需要更难提供的东西。

让基本的反馈回路运转起来——Agent 可以运行的测试、给出可解析输出的 CI、指向修复方案的错误信息——这是基本要求。

[Karpathy](https://x.com/karpathy)：“工作的未来是 AI 工程师为其他 AI  Agent 设计环境和反馈循环。” 他让 16 个并行的 Agent 构建了一个 C 编译器，但精心设计了测试基础设施。“我的大部分精力都花在了为 Claude 设计环境上——测试、环境、反馈。”

更难的问题是校准特定于你的系统的传感器和执行器。这是大多数人卡住的地方，也是他们责怪 Agent 的地方。

“它一直在做错事。它不理解我们的代码库。” 这个诊断几乎总是错的。Agent 失败不是因为能力不足。它失败是因为它需要的知识——在你的系统中什么是“好”，你的架构奖励什么模式、避免什么——锁在你的脑子里，而你还没有将其外化。Agent 不会通过渗透学习。如果你不写下来，Agent 在第一百次运行时和第一次运行时犯同样的错误。

这项工作就是让你的判断力变得可机器读取。描述实际分层和依赖方向的架构文档。内含修复说明的自定义 Linter。编码团队品味的黄金原则。

[OpenAI](https://openai.com/)：他们每周五花 20% 的时间清理“AI 垃圾”——直到他们将标准编码到 Harness 本身中。

这些实践所要求的——文档、自动化测试、编纂的架构决策、快速反馈回路——一直是正确的。过去三十年写的每本工程书都推荐它们。大多数人跳过它们是因为跳过它的代价是缓慢且弥散的：质量逐渐下降、痛苦的入职、悄悄累积的技术债务。

Agentic 工程使代价变得极端。跳过文档，Agent 会忽略你的约定——不是在一个 PR 上，而是在每个 PR 上，以机器速度全天候运行。跳过测试，反馈回路根本无法闭合。跳过架构约束，漂移累积的速度超过你修复它的速度。而且这是一个陷阱：如果 Agent 不知道干净是什么样，你就不能用 Agent 来清理烂摊子。没有校准，创造问题的机器也无法解决问题。

**实践没有改变。忽视它们的代价已经变得无法忍受。**

生成-验证不对称——Cobbe 等人的 [LLM-as-Judge](https://arxiv.org/abs/2306.05685) 背后的直觉——指明了方向。生成正确的解决方案比验证一个更难。你不需要超越机器实现。你需要超越它进行评估：指定“正确”是什么样子，识别输出何时遗漏，判断方向是否正确。

设计瓦特调速器的工人们没有回去转动阀门。不是因为他们不能。因为那已经没有意义了。