# 验证经济：我们解决了生成问题，然后呢？

两年来，关于 AI 颠覆软件工程的讨论一直遵循着相同的故事情节：模型变得越来越聪明，基准测试的成绩不断攀升。

那是错误的视角。

过去两个月里发生的范式转变，几乎与智能毫无关系。它与 AI “居住”在哪里，以及它能接触到什么有关。

## 界面的转变

直到 2025 年 12 月，AI 编码工具还只生活在 IDE（集成开发环境）中。它们坐在你旁边，提出建议。由你决定运行什么。你阅读报错信息。你思考下一步该尝试什么。

很有用，但本质上是被动的。AI 根本不知道系统是否还在正常工作。它不能运行程序，不能看到故障，也不能再试一次。这个循环仍然是由人类掌控的。

像 codex cli 和 claude code 这样的工具改变了这一点。它们将 AI 移出了编辑器，带入了终端。现在，模型自己读取代码库、运行命令、执行测试、检查故障、修补代码，然后再试一次。它不需要等你。

这听起来像只是工具存在位置的一个小变化。但其实不是。

## 真正的工作从来不是写代码

这是大多数人对软件工程师实际工作内容的误解。

写代码只是可见的输出。真正的工作是围绕它的整个过程：阅读系统、运行测试、看看哪里坏了、形成假设、再试一次。

这个循环不是一个推理问题，它是一个搜索问题。你不断运行实验，直到有个方法奏效。你运行实验的速度越快，你收敛（找到答案）的速度就越快。

人类工程师在迷失思路之前，能逐步尝试三到四个假设。而一个智能体（agent）在你读完第一条报错信息之前，就已经运行了几十个。

一旦机器执行这个过程的速度超过人类，结构性的变化就发生了。中间层变得廉价：样板代码、迁移、胶水代码、常规调试。价值开始向两端转移。

向上转移至：架构、抽象、定义系统应该是什么的决策。
向下转移至：定义什么是“正确”的基础设施。

## 验证的鸿沟

但这种转变暴露了大多数团队尚未准备好面对的一个问题。

测试（testing）检查的是代码是否按照你想象的那样运行。而验证（verification）检查的是你认为代码应该做的事，是否真正是你想要的。你所指定的和你所意指的之间的鸿沟，正是智能体失败的地方。它们用通过你测试套件却搞垮你系统的方式失败。

智能体非常擅长满足明确的约束条件。但它们很不擅长知道你忘记写下哪些约束。它们能找到通过测试的解决方案，但它们不知道测试遗漏了什么。

人类工程师携带着代码库中不包含的上下文。机构记忆。架构意图。两年前做出某个决定但并没有写在任何地方的原因。智能体没有这些。它只有代码库里的内容和你告诉它要优化的目标。

所以它进行优化。非常高效地。有时还会朝着你意想不到的方向。

## 价值真正所在

瓶颈转移了：不是从人类到机器，不是从慢到快。而是从生成（generation）转移到了验证（verification）。

稀缺资源不再是产生输出。现在每个人都能产生输出。稀缺资源是证明输出在触及任何真实事物之前是值得信赖的基础设施。

测试真实工作流的评估框架。在生产环境前捕捉退化的部署网关。基于“事情必然会出错”假设建立的回滚逻辑。智能体采取的每一个行动的审计跟踪。限制爆炸半径的权限边界。

弄明白这些的团队，并不是那些生成速度最快的团队。而是那些构建了管控生成器输出成果的控制层团队：控制平面、评估系统、以及让 AI 能够在真实系统中大规模运作的信任基础设施。

## 工作的层级提升了

软件工程师并没有消失。只是角色发生了转变：从编写代码，转变为治理编写代码的系统。从管理循环，转变为定义这个循环的目的。

在这场变革中获胜的公司，不是在押注更好的模型。而是在押注更好的验证。

外部世界的大多数人还没注意到这一点。而在这里建设的人们，没有奢侈的时间等到他们都注意到。