# MCP vs. Skills for AI agents，一文讲透！

URL: https://x.com/akshay_pachaar/status/2029534926828388537

人们经常把 MCP 和 Skills（技能）当成同一回事。

但它们不是。

把它们混为一谈，是我在人们开始认真构建 AI 智能体（AI agents）时看到的最常见的错误之一。

所以，让我们从头开始把这两个概念掰开揉碎地讲清楚。

在 MCP 存在之前，将 AI 模型连接到外部工具意味着每次都需要编写自定义的集成代码。
10 个模型，100 个工具？那就是要构建和维护 1,000 个独一无二的连接器。AI 工具生态系统曾经就是一团乱麻，充斥着一次性的胶水代码。

MCP (Model Context Protocol，模型上下文协议) 通过一个共享的通信标准解决了这个问题。每个工具都成为一个“服务器 (server)”，暴露它能做什么。每个 AI 智能体都成为一个“客户端 (client)”，知道该如何去提问。它们通过一个干净、定义明确的接口，用结构化的 JSON 消息进行对话。

只需构建一次 GitHub MCP 服务器，它就能与 Claude、ChatGPT、Cursor，或任何其他能够理解 MCP 的智能体配合工作。这就是它的核心价值：**编写一次集成，到处都可以使用。**

但是，大多数的解释到这里就戛然而止了。

MCP 解决的是**连接 (connection)**问题。它并没有解决**使用 (usage)**问题。

你可以塞给一个智能体 50 个完美对接的 MCP 工具，但如果它不知道在什么时候调用哪个工具、以什么顺序调用，以及需要什么上下文，它的表现依然会很差。

这正是 Skills 要填补的空白。

一个 Skill（技能）是一个可移植的程序性知识包。你可以把一个 `SKILL.md` 文件想象成不仅在告诉智能体“这是你的工具”，还在告诉它“对于这个特定的任务，你该如何使用它们。”

- 一个写作技能，打包了语气指南和输出模板。
- 一个代码审查技能，打包了需要检查的模式和需要遵循的规则。

MCP 赋予了智能体双手。而 Skills 赋予了它肌肉记忆。

它们组合在一起，形成了一个生产级 AI 智能体的完整能力栈：
- **MCP** 处理工具连接性（接线层）
- **Skills** 处理任务执行（知识层）
- **Agent (智能体)** 使用它的上下文和推理能力来协调这两者

这就是为什么高级的智能体设置越来越多地同时搭载这两者：用于集成的 MCP 服务器，以及用于提供领域专业知识的 `SKILL.md` 文件。

如果你正在构建智能体，我分享了一个包含 8.5 万多个技能的仓库，你可以将其与任何智能体一起使用，链接在下一条推文中！