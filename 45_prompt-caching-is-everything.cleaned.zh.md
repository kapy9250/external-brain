# 构建 Claude Code 的经验教训：Prompt Caching 就是一切

在工程领域，常有一句话叫 "Cache Rules Everything Around Me"（缓存统治着我周围的一切），同样的规则也适用于 Agent。

像 Claude Code 这样长时间运行的 Agentic 产品之所以可行，正是因为 Prompt Caching（提示词缓存），它允许我们重用之前往返的计算结果，并显著降低延迟和成本。

什么是 Prompt Caching？它是如何工作的？以及在技术上如何实现它？

在 Claude Code，我们围绕 Prompt Caching 构建了整个 Harness（运行框架）。高缓存命中率降低了成本，并帮助我们为订阅计划创造更宽裕的速率限制，因此我们对缓存命中率设置了告警，如果命中率太低就会申报 SEV（严重故障）。

以下是我们从大规模优化 Prompt Caching 中学到的（往往是反直觉的）经验教训。

[![Image 1: Image](./assets/prompt-caching-is-everything/image-01.jpg)](https://x.com/trq212/article/2024574133011673516/media/2024553977430646784)

Prompt Caching 的工作原理是前缀匹配 —— API 会缓存从请求开始到每一个 `cache_control` 断点的所有内容。这意味着你放置内容的顺序至关重要，你希望尽可能多的请求能够共享相同的前缀。

最好的方法是：静态内容放前面，动态内容放后面。对于 Claude Code 来说，这看起来像：

1.  静态的系统提示词 & Tools（全局缓存）
2.  Claude.MD（在项目中缓存）
3.  Session 上下文（在会话中缓存）
4.  对话消息

通过这种方式，我们能最大化共享缓存命中的会话数量。

但这可能出奇地脆弱！我们之前破坏这种排序的原因包括：在静态的系统提示词中放入精确的时间戳、非确定性地打乱 Tool 定义的顺序、更新 Tool 的参数（例如 AgentTool 可以调用哪些 Agent）等。

有时候你放在 prompt 里的信息可能会过时，例如时间变了，或者用户修改了一个文件。你可能会忍不住去更新 prompt，但这会导致缓存未命中 (cache miss)，并且最终可能会让用户付出高昂的成本。

考虑你是否能在下一轮的对话消息中传递这些信息。在 Claude Code 中，我们在下一个用户消息或 Tool 结果中添加一个 `<system-reminder>` 标签，为模型提供更新后的信息（例如：现在是周三了），这有助于保留缓存。

Prompt 缓存对不同的模型是独立的，这使得缓存的数学计算变得非常反直觉。

如果你在使用 Opus 的对话中已经消耗了 10 万个 token，这时你想问一个很容易回答的问题，切换到 Haiku 实际上会比让 Opus 回答更昂贵，因为我们需要为 Haiku 重新构建 Prompt 缓存。

如果你需要切换模型，最好的方法是使用 Subagents（子 Agent），由 Opus 准备一条“移交 (handoff)”消息给另一个模型，告知它需要完成的任务。我们在 Claude Code 中的 Explore Agent 经常这样做（它们使用 Haiku）。

在对话中途改变 Tool 集合是人们破坏 Prompt Caching 最常见的方式之一。这似乎很直观 —— 你应该只给模型它现在需要的工具。但是因为 Tools 是缓存前缀的一部分，添加或移除一个 Tool 会使整个对话的缓存失效。

**Plan Mode (计划模式) — 围绕缓存进行设计**

Plan Mode 是一个围绕缓存约束设计功能的绝佳例子。直观的做法是：当用户进入 Plan Mode 时，将 Tool 集合替换为只包含只读的 Tool。但那样会破坏缓存。

相反，我们在请求中始终保留所有的 Tool，并将 `EnterPlanMode` 和 `ExitPlanMode` 本身作为 Tool。当用户开启 Plan Mode 时，Agent 会收到一条系统消息，说明它正处于 Plan Mode 及其指令 —— 探索代码库、不要编辑文件、计划完成后调用 `ExitPlanMode`。Tool 的定义永远不会改变。

这带来了一个额外的好处：因为 `EnterPlanMode` 是模型可以自己调用的 Tool，当它检测到一个难题时，它可以自主进入 Plan Mode，而不会发生任何缓存中断。

**Tool Search (工具搜索) — 延迟加载而不是移除**

同样的原则也适用于我们的 Tool 搜索功能。Claude Code 可能会加载几十个 MCP Tools，如果在每个请求中都包含所有这些 Tools 将会非常昂贵。但是在对话中途移除它们会破坏缓存。

我们的解决方案：`defer_loading`。我们不是移除 Tools，而是发送轻量级的存根 (stubs) —— 只有 Tool 的名称，带有 `defer_loading: true` —— 模型可以在需要时通过 `ToolSearch` 这个 Tool 来“发现”它们。完整的 Tool Schema 仅在模型选择它们时才会加载。这保持了缓存前缀的稳定：相同的存根始终以相同的顺序存在。

幸运的是，你可以通过我们的 API 使用该 Tool 来简化这个过程。

[![Image 2: Image](./assets/prompt-caching-is-everything/image-02.jpg)](https://x.com/trq212/article/2024574133011673516/media/2024558324591841283)

压缩 (Compaction) 是当你耗尽上下文窗口时发生的事情。我们会总结迄今为止的对话，并使用该总结继续一个新的会话。

令人惊讶的是，压缩在 Prompt Caching 方面有许多反直觉的边缘情况。

特别是在进行压缩时，我们需要将整个对话发送给模型以生成总结。如果你使用不同的系统提示词和零个 Tool 来进行一次单独的 API 调用（这是一种简单的实现），这与主对话的缓存前缀就完全不匹配了。你需要为所有这些输入 token 支付全价，这极大地增加了用户的成本。

**解决方案 — 缓存安全的分叉 (Cache-Safe Forking)**

当我们运行压缩时，我们使用与父对话完全相同的系统提示词、用户上下文、系统上下文和 Tool 定义。我们将父对话的消息预置在前面，然后将压缩提示词作为一条新的用户消息追加在最后。

从 API 的角度来看，这个请求看起来几乎与父对话的最后一个请求完全相同 —— 相同的前缀、相同的 Tool、相同的历史记录 —— 因此重复使用了缓存的前缀。唯一新增的 token 就是压缩提示词本身。

然而，这确实意味着我们需要保留一个“压缩缓冲区 (compaction buffer)”，以便在上下文窗口中有足够的空间来容纳压缩消息和总结输出的 token。

压缩很棘手，但幸运的是，你不需要自己去学习这些教训 —— 基于我们在 Claude Code 中学到的知识，我们已将其直接构建到了 API 中，因此你可以在自己的应用程序中应用这些模式。

1.  **Prompt Caching 是前缀匹配。** 前缀中任何地方的任何改变都会使其后的所有内容失效。围绕这个约束来设计你的整个系统。把排序做对了，大部分缓存就能免费生效。
2.  **使用消息代替修改系统提示词。** 你可能会忍不住去编辑系统提示词来执行诸如进入计划模式、更改日期等操作，但更好的做法实际上是将这些作为消息插入到对话过程中。
3.  **不要在对话中途改变 Tool 或模型。** 使用 Tool 来模拟状态转换（如计划模式），而不是改变 Tool 集合。使用延迟加载而不是移除 Tool。
4.  **像监控正常运行时间 (uptime) 一样监控你的缓存命中率。** 我们对缓存中断设置告警，并将其视为事故。几个百分点的缓存未命中率都会极大地影响成本和延迟。
5.  **Fork (分叉) 操作需要共享父级的前缀。** 如果你需要运行一个分支计算（压缩、总结、技能执行），使用完全相同的缓存安全参数，这样你就能在父级前缀上获得缓存命中。

Claude Code 从第一天起就是围绕 Prompt Caching 构建的，如果你在构建一个 Agent，你也应该这么做。